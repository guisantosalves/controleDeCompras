function setConfig(){
    var texts = {
        "title": "Shopping controll" 
    };

    document.title = texts.title;
    document.getElementById("navTittle").innerHTML = texts.title;
}

setConfig();